const IndexPage = require('../page/IndexPage');

describe('index page', () => {
	let indexPage;
	
	before(() => {
		indexPage = new IndexPage();
		indexPage.open();
	});
	
	it('test', () => {
		
	});
});
